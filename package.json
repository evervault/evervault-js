{
  "name": "evervault-browser-sdk",
  "version": "1.0.0",
  "description": "",
  "main": "./lib/v1/index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "prettier --check \"./**/*.js\"",
    "build": "./scripts/build.sh",
    "dev": "npm run build && php -S localhost:5000"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "prettier": "^2.8.0",
    "webpack": "^5.0.0",
    "webpack-cli": "^5.0.0"
  }
}
