{
  "name": "evervault-browser-sdk",
  "version": "1.0.0",
  "description": "",
  "main": "./lib/index.js",
  "engines": {
    "node": ">=16 <=19",
    "pnpm": "~7"
  },
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "test": "mocha",
    "lint": "prettier --check \"./**/*.js\"",
    "build": "webpack --mode=production --config=webpack.config.js",
    "dev": "pnpm run build && php -S localhost:5000",
    "test-serve": "pnpm run test-serve:build && serve -p 3000 ./e2e-app",
    "test-serve:build": "pnpm run build && cp ./dist/index.js ./e2e-app/evervault.bundle.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "dotenv": "^16.0.3",
    "jsdom": "^20.0.3",
    "mocha": "^10.2.0",
    "nock": "^13.3.0",
    "prettier": "^2.0.5",
    "web-file-polyfill": "^1.0.4",
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.12",
    "serve": "^14.2.0"
  }
}
