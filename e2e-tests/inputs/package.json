{
    "private": true,
    "version": "0.0.0",
    "name": "@evervault/inputs-e2e-tests",
    "scripts": {
        "dev:test": "playwright test",
        "e2e:test": "start-server-and-test \"cd ../../packages/inputs && vite preview --port 3020\" http://localhost:3020 \"playwright test\""
    },
    "dependencies": {
        "@playwright/test": "^1.32.2",
        "start-server-and-test": "^2.0.0",
        "@evervault/inputs": "workspace:*"
    }
}