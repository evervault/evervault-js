---
"@evervault/ui-components": patch
---

Force a fresh release. The @evervault/ui-components@1.44.0 release (from #1058) was cancelled mid-flight when the Playwright Docker image mismatch failed a sibling release job, so it never deployed to production. The Docker image is now fixed (#1061).
