---
"@evervault/browser": minor
"@evervault/js": minor
---

Add optional Apple Pay `billingContact` / `shippingContact` prefill on the web SDK. Merchants can pass known buyer contacts via `ApplePayButtonOptions`; they are set on the Apple Pay PaymentRequest method data so the sheet shows them as defaults.
