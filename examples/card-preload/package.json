{
  "name": "example-card-preload",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 4050",
    "build": "vite build",
    "clean": "rm -rf node_modules dist"
  },
  "dependencies": {
    "@evervault/js": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vite": "catalog:"
  }
}
